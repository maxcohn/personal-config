# uBlock

This is my uBlock Origin config.
This is trickier to sync because it's a browser extension and I don't believe I can modify a file directly.

To sync, drop this into the extension's filter page.
